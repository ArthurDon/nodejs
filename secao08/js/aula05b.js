function removerBotao(btn){
    // Recuperar o elemento div
    // let divElement = document.querySelector('#app'); // id app
   

    // Remove o filho selecionado da div
    // divElement.removeChild(btn)

    document.querySelector('#app').removeChild(btn);// pega o id que é o app, do div, remove os filhos que é o btn
}